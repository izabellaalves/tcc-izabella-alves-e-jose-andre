# TCC Feature Engineering - Python Dependencies
# Python 3.8+

# Data processing
pandas>=2.0.0
numpy>=1.24.0

# Type hints and dataclasses (backport para Python < 3.10)
typing-extensions>=4.5.0

# Logging e utilities
python-dateutil>=2.8.0

# CSV handling (já incluso no pandas, mas explícito)
# csv - built-in

# Path manipulation (já incluso no Python 3.4+)
# pathlib - built-in

# Subprocess e system calls (built-in)
# subprocess - built-in

# Progress bar (opcional, para feedback visual)
tqdm>=4.65.0

# Para análise exploratória posterior (opcional)
matplotlib>=3.7.0
seaborn>=0.12.0
scikit-learn>=1.3.0
